import 'package:flutter/material.dart';

class TachesScreen extends StatelessWidget {
  const TachesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Tâches')),
    );
  }
}